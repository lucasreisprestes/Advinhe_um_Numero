import java.util.Random;

import java.util.Scanner;


public class Main {
	/*
	 * Crie um programa para jogar "Adivinhe o número". A ideia do jogo é adivinhar 
	 * um número entre 1 e 1000.
	 * O programa deve primeiramente perguntar ao usuÃ¡rio se o jogo é para 1 ou 2 jogadores. 
	 * Caso seja para 1 jogador, o programa deve escolher um número aleatório entre 1 e 1000 
	 * e o usuÃ¡rio deve adivinhar (verificar a documentação da classe Random).
	 * 
	 * Caso seja para 2 jogadores, o primeiro usuário deve digitar um nÃºmero entre 1 e 1000 
	 * para que o segundo usuário adivinhe.
	 * ApÃ³s a escolha do numero, o programa deve pedir um nÃºmero para o usuÃ¡rio atÃ© 
	 * que ele acerte. Para isto, a cada numero digitado pelo usuÃ¡rio o programa deve avisar se:
	 * - O numero digitado é maior que o numero a ser adivinhado
	 * - O numero digitado é menor que o numero a ser adivinhado
	 * - O usuário acertou o numero. Neste caso, deve exibir a quantidade de tentativas 
	 * até que o acerto.
	 */

	/**
	 * @param args
	 */
	
	public static void main(String[] args) {
				
		
		Game game = new Game();
	
	}

}
